[package]
name = "video-codec"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "Pure Rust types for video codec configuration, errors, and frame info"

[dependencies]
thiserror = "2.0.18"
